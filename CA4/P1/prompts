for email is this correct?: [a-zA-Z0-9]+@[a-zA-Z0-9]+.(com | org)

----------------------------------------------------------------------------------------------------------------------------------

for phone number: ^(+98)[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]

-----------------------------------------------------------------------------------------------------------------------------------

r'^[A-Z][A-Z][0-9]{2,4}[a-z]?(-v[1-9][0-9]?)?

is this okay for product code?

-----------------------------------------------------------------------------------------------------------------------------------

r"^[Ss]top$" 
is this okay for stop word?

-----------------------------------------------------------------------------------------------------------------------------------

what about r"\W[Ss]top\W"

-----------------------------------------------------------------------------------------------------------------------------------

r'(some students.*some students) | (many employees.*many employees)'

is this okay for ‫‪repeated phrase?

-----------------------------------------------------------------------------------------------------------------------------------

r'^[1-9][0-9]{3}/([1-6]/[1-9]|([1-2][0-9])|(3[01]))|([6-9]|(1[0-2])/[1-9]|([1-2][0-9])|(30))$'

is this okay for date?

-----------------------------------------------------------------------------------------------------------------------------------

r'^(['"])[a-zA-Z0-9 ]*\1$"

is this okay for the quotation part?

-----------------------------------------------------------------------------------------------------------------------------------

how to do web scrapping with regex? what are its challenges?














































































































-
